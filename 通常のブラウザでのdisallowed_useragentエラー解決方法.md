# 通常のブラウザでのdisallowed_useragentエラー解決方法

## 🔴 状況

- ✅ 通常のブラウザ（Safari、Chrome）で開いている
- ✅ アプリ内ブラウザ（WebView）ではない
- ❌ それでもエラー403: disallowed_useragentが発生

## 🔍 追加で確認すべき項目

### 1. OAuth同意画面のテストユーザー設定（最重要）

**テストモードの場合、テストユーザーが追加されていないとログインできません。**

#### 確認手順

1. [Google Cloud Console](https://console.cloud.google.com/) にアクセス
2. プロジェクト「**ikerukane-app**」を選択
3. 「**APIとサービス**」→「**OAuth同意画面**」を選択
4. 「**テストユーザー**」セクションを確認

#### テストユーザーを追加

1. 「**テストユーザー**」セクションで「**ユーザーを追加**」をクリック
2. ログインに使用するGoogleアカウントのメールアドレスを追加：
   - `suikanoasaduke@gmail.com`
   - `uriuri4321@gmail.com`
   - その他のテスト用アカウント
3. 「**追加**」をクリック

**重要**: テストモードでは、追加したテストユーザー以外はログインできません。

### 2. OAuth同意画面の公開状態を確認

#### テストモード vs 本番モード

**テストモード**:
- テストユーザーのみログイン可能（最大100人）
- 一般ユーザーはログインできない
- 開発中はテストモードで十分

**本番モード**:
- すべてのユーザーがログイン可能
- Googleの審査が必要
- 審査が完了していない場合、一般ユーザーはログインできない

#### 確認方法

1. Google Cloud Console > OAuth同意画面
2. 画面の上部に「**テストモード**」または「**本番モード**」と表示されている
3. テストモードの場合、テストユーザーが追加されているか確認

### 3. ブラウザのUser-Agentを確認

#### 確認方法

1. ブラウザの開発者ツールを開く（F12または右クリック > 検証）
2. 「Console」タブを開く
3. 以下を実行：

```javascript
console.log(navigator.userAgent);
```

#### 確認すべき点

- `WebView` という文字列が含まれていないか
- `Line`、`Twitter`、`Facebook` などの文字列が含まれていないか
- ブラウザの拡張機能がUser-Agentを変更していないか

### 4. ブラウザの拡張機能を無効化

ブラウザの拡張機能がUser-Agentを変更している可能性があります。

#### Chromeの場合

1. シークレットモードで開く（拡張機能が無効になる）
2. または、拡張機能を一時的に無効化

#### Safariの場合

1. プライベートブラウジングで開く
2. または、拡張機能を一時的に無効化

### 5. 別のブラウザで試す

1. SafariとChromeの両方で試す
2. どちらでも同じエラーが発生するか確認
3. 異なる結果が出る場合、ブラウザ固有の問題の可能性

### 6. モバイルブラウザの場合

#### iOS Safariの場合

- 「デスクトップサイトを表示」モードが有効になっていないか確認
- 通常のモバイルモードで試す

#### Android Chromeの場合

- 「デスクトップサイト」モードが有効になっていないか確認
- 通常のモバイルモードで試す

### 7. OAuth同意画面のスコープ設定を確認

#### 確認手順

1. Google Cloud Console > OAuth同意画面
2. 「**スコープ**」セクションを確認
3. 以下のスコープが追加されているか確認：
   - `openid`
   - `email`
   - `profile`

#### スコープを追加

1. 「**スコープを追加または削除**」をクリック
2. 上記のスコープを追加
3. 「**更新**」をクリック

### 8. Firebase Authenticationの設定を再確認

#### 確認手順

1. [Firebase Console](https://console.firebase.google.com/) にアクセス
2. プロジェクト「**ikerukane-app**」を選択
3. 「**Authentication**」→「**Sign-in method**」を選択
4. 「**Google**」をクリック

#### 確認項目

- ✅ **有効**になっているか
- ✅ **プロジェクトのサポートメール**が設定されているか
- ✅ **OAuth同意画面のURL**が正しく設定されているか

### 9. ブラウザのキャッシュとCookieを完全にクリア

#### Safariの場合

1. 設定 > Safari
2. 「履歴とWebサイトデータを消去」をタップ
3. 「消去」を確認
4. ブラウザを再起動

#### Chromeの場合

1. 設定 > プライバシーとセキュリティ
2. 「閲覧履歴データの削除」をクリック
3. 「時間の範囲」を「全期間」に設定
4. 「Cookieとサイトデータ」を選択
5. 「データを削除」をクリック
6. ブラウザを再起動

### 10. シークレット/プライベートモードで試す

キャッシュやCookie、拡張機能の影響を完全に排除：

1. シークレット/プライベートモードでブラウザを開く
2. `https://uriuri4321-eng.github.io` にアクセス
3. Googleログインを試す

---

## 🔧 段階的な診断手順

### ステップ1: OAuth同意画面のテストユーザーを確認（最重要）

1. **Google Cloud Console > OAuth同意画面 > テストユーザー**
2. **ログインに使用するGoogleアカウントが追加されているか確認**
3. **追加されていない場合、追加する**

### ステップ2: ブラウザの拡張機能を無効化

1. **シークレット/プライベートモードで試す**
2. **または、拡張機能を一時的に無効化**

### ステップ3: 別のブラウザで試す

1. **SafariとChromeの両方で試す**
2. **どちらでも同じエラーが発生するか確認**

### ステップ4: ブラウザのキャッシュを完全にクリア

1. **ブラウザのキャッシュとCookieを完全にクリア**
2. **ブラウザを再起動**

---

## 📝 確認チェックリスト

以下の項目を順番に確認してください：

- [ ] OAuth同意画面でテストユーザーが追加されている（**最重要**）
- [ ] ログインに使用するGoogleアカウントがテストユーザーに含まれている
- [ ] OAuth同意画面のスコープ（`openid`、`email`、`profile`）が追加されている
- [ ] シークレット/プライベートモードで試した
- [ ] ブラウザの拡張機能を無効化して試した
- [ ] 別のブラウザ（SafariとChrome）で試した
- [ ] ブラウザのキャッシュとCookieを完全にクリアした
- [ ] ブラウザを再起動した

---

## 🎯 最も可能性が高い原因

### テストユーザーが追加されていない

**テストモードの場合、テストユーザーが追加されていないと、通常のブラウザからでもログインできません。**

**解決策**:
1. Google Cloud Console > OAuth同意画面 > テストユーザー
2. ログインに使用するGoogleアカウントのメールアドレスを追加
3. 保存して再度ログインを試す

---

## 📞 さらなるサポート

上記の手順を試しても解決しない場合：

1. **エラーメッセージの全文を確認**
   - ブラウザの開発者ツール（F12）> Consoleタブでエラーを確認
   - エラーメッセージのスクリーンショットを保存

2. **User-Agentを確認**
   - 開発者ツールのConsoleで `navigator.userAgent` を実行
   - 結果を確認

3. **Firebase サポートに問い合わせ**
   - [Firebase サポート](https://firebase.google.com/support)

---

**最終更新日**: 2025年1月

